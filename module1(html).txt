(1)are the html tag and element the same thing?
   
    html tag are used to hold the html element.html element hold the content.

(2)what are tag and attributes html?

   html tag are used to hold the html element.html element hold the content.html attributes are used to describe the characteristic of an
   html element in detail.

(3)what are void element in html?

  void element does not have ending tag and can only have attributes but do not cointain any kind of cointain these element cointain can
  have blackslash before ending of start tag but that is completely optional.ex:<br>,<hr>,<img>,<input>,<link>.

(4)what are html entities?

   html entities is a piece of text ("string") that begins with an ampersand & and end with a semicolon ;.entities are frequently used 
   to display reserved characters (which would otherwise be interpreted as html code),and invisible characters(like non-breaking spaces)

(5)what are different type of list in html?
  
   there are three types of list in html.
    >unorder list.
    >order list.
    >description list

(6)what is the 'class' attributes in html?

   the class attributes specifies one or more classnames for an element.the class attributes is mostly used to point to a class in a 
   style sheet.

(7)what is different between the id attributes and class attributes of html element?

   the only different between them is that id is unique in a page and can only apply to at most one element while class selector can
   apply to multiple element.

(8)what are various formating tag in html?

   <b>-bold text.
   <strong>-important text.
   <i>-italic text.
   <em>-emphasized text.
   <mark>-marked text.
   <ins>-inserted text.

(9)how is cell padding different from cell spacing?

   cellpadding basically defines the space present brtween a table cells border and the content present in it.cellspacing basically 
   defines the space present between individual adjacent cells.

(10)how can we club two or more row or column into a single row and column in an html table?

    you can merge two or more table cells in a column using the colspan attributes in a column using the colspan attributes in a <td>
    html tag (table data).to merge two or more row cells,use the rowspan attribute.

(11)what is different between a block level element and an inline element?

    block element occupy the full width irrespective of their sufficiency.inline element don't start in a new line.block element
    always start in a new line.inline element allow other inline element to sit behind.

(12)how to create a hyperlink in html?
    
    hyper link in an html page use the <a> and  </a> tag,which are the tags used to define the link.the <a> tag indicates where text 
    gets added inside these tags,will work as a hyperlink.add the url for the link in the <a href="">.

(13)what is the use of an iframe tag?
   
    the <iframe> tag specifies an inline frame.an inline frame is used to embed another document within the current html document.

(14)what is the use of an a span tag?explain with example?

    the <span> tag is an inline container used to mark up a part of a text,or a part of a document.the <span> tag is easily styled by
    css or manipulation with javascript using the class or id attributes.the <span> tag is much like the <div>,element but <div> ia a 
    block-level element and <span> is an inline element.
    ex:
    <p>my mother has<span style="color:blue">blue</span>eyes.</p>

(15)how to insert a picture into a background image of a web page?

    to set the background image of a webpage,use the css style.under the css<style>tag,add the property background-image.the property
    sets a graphics such as jpg,png,svg,gif,etc.

(16)how are active link different from normal link?

    some browser recognize an active link when the mouse cursor is placed over that link; others recognize active link when the link
    has the focus.those that don't have a mouse cursor over that link is consider a normal link.

 (17)what are the different tags to separate section of text?

    the <br> tag is one way to separate the lines of text.there are other tags like the <p> tag and <blockquote> tag that are also
    used to separate section of text.

  (18)what is svg?

     >svg stand for scalable vector graphics
     >svg is used to define graphics for the web
     >svg is a W3C recommendation

   (19)what is different between html and xhtml?

                      xhtml                                                                         html
                      
       >xhtml stand for extensible hypertext markup language.  >html is hypertex markup language which is most widely used language 
       >it can be part of the xml markup language this is       over the internet.
        because of xhtml have feature of both xml and html.    >html is used to create web pages and link them from one to another.
       >xhtml is extended from xml and html.                   >html is not a programing language,but it is markup language.
       >xhtml can be consider as a better version of html.     >we can use different other technology as like css and javascript to
                                                                give a new look to the pages develope by html.

   (20)what is logical and physical tag in html?

       logical tags describe the behaviour,nature of content for the next enclosed inside the text enclosed inside the text.they repre
       -sent the function of text on the page.physical tags are used to decide the appearance of the text and do not provide any info
       -rmation about the text.

     


























 



























